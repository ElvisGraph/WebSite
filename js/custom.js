jQuery(document).ready(function($){$('.search-form input[type=text]').focus();if($('.items-list').length){$('.items-list').masonry({itemSelector:'.item'});}
$('.btn-play').on('click',function(){var data_id=$(this).attr('data-id'),data_title=$(this).attr('data-title'),data_source=$(this).attr('data-source');$('.player').html('');$('.btn-play').show();$('.btn-stop').hide();if(window.XMLHttpRequest){xmlhttp=new XMLHttpRequest()}else{xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");}
xmlhttp.open("GET",dsz.url+"/player/"+data_source+"/"+data_id+"/");xmlhttp.onreadystatechange=function(){if(xmlhttp.readyState==4&&xmlhttp.status==200){$('#player-'+data_id).html(xmlhttp.responseText);$(this).text("Stop");}};xmlhttp.send(null);$(this).hide();$('#item-'+data_id+' .btn-stop').show();});$('.btn-stop').on('click',function(){var data_id=$(this).attr('data-id');$('.player').html('');$(this).hide();$('.btn-play').show();});});